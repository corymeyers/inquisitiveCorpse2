{{#if updateCityForm}}
  <div>
      <form>
        <div class="form-group">
          <label for="name">Your name:</label>
          {{input value=name id="name"}}
        </div>

        <div class="form-group">
          <label for="country">Change question to:</label>
          {{input value=country id="country"}}
        </div>

        <button {{action 'update' city}}>Save</button>
      </form>
    </div>
{{else}}
    <button {{action 'updateCityForm'}}>Change question</button>
{{/if}}
